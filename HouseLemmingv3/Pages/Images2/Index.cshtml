@page
@model HouseLemmingv3.Pages.Images2.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Image[0].FileName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Image[0].Advert)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Image[0].ImageBytes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Image) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FileName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Advert.AddrCity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImageBytes)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ImageId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ImageId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ImageId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
